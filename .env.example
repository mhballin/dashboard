# Frontend build/runtime config (Vite)
# Leave empty in local dev so the app uses relative /api and Vite proxy.
VITE_API_URL=

# Vite dev server proxy target.
# Use 127.0.0.1 to avoid localhost DNS edge cases.
VITE_API_PROXY_TARGET=http://127.0.0.1:3001
